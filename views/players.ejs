<!DOCTYPE html>
<html lang="en">

<%- include('partials/head', { title: 'Players | Pokemath' }) %>

<body>
    <%- include('partials/nav', { username }) %>

    <div class="players">
        <% for (let p of players) { %>
            <a href="/<%= p.username %>"><p class="player"><%= p.username %></p></a>
        <% } %>
    </div>
</body>
</html>
